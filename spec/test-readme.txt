1. Match design in HTML/CSS and Angular.JS using Bootstrap. \
2. Customize Bootstrap to match styles via a LESS/SASS build with variable modifications. \
3. Show functional tabs, no content necessary on other pages (just FPO/lorem ipsum is fine) \
4. Use icon font or SVGs where necessary. \
5. Build the social media account inputs and icons using a JSON file.\
6. Build the functionality for the Business Contacts page using a JSON file that contains a sample primary address. The interface should allow addresses to be added, edited, or removed, but there should always be at least one primary address at all times.